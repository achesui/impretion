{
	"name": "@worker/consumer-billing-balance",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --port 3002 --persist-to=../../shared-workers-data",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	}
}
